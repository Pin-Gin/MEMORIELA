# MEMORIELA Website Editing Rules

この文書は MEMORIELA 公式サイトの編集作業における必須ルールです。
新しいチャットや作業セッションでホームページ編集を行う場合、作業前にこの文書を確認し、このルールを優先して適用します。

## 1. ホームページ作業中の画像生成は絶対禁止

MEMORIELA公式ホームページに関する作業中は、**新規画像生成を一切行わない**。

これは提案・確認・レイアウト調整・Hero検討・MASTER参照・画像差し替え・削除・配置変更を含む、すべてのホームページ作業に適用する。

- `image_gen` を呼ばない。
- 「この画像で」「このHeroで」「これを使う」「画像を消す」「画像を差し替える」「この構図に合わせる」は画像生成依頼ではない。
- 画像が不足していても、AI側の判断で補完生成しない。
- MASTERを再生成しない。
- 既存画像の代わりになる画像を勝手に作らない。
- ユーザーがホームページ作業中に画像について言及しただけでは、画像生成へ切り替えない。

ホームページ作業から離れた**別の明示的な画像制作依頼**として、ユーザーが「画像を生成して」と明確に指示し、かつこの禁止を明示的に解除した場合のみ、新規画像生成を扱う。

既存画像の編集は新規生成と分離して扱う。ユーザーが「この画像を編集して」「文字を消して」など、画像そのものの編集を明示した場合に限り編集可能とする。

ユーザーが「ホームページ」「サイト」「レイアウト」「位置」「大きさ」「表示」「削除」「CSS」「Hero」「セクション」「イメージ図に合わせる」など、Webサイト上の変更を指示した場合、それは **Web実装作業** として扱う。

この場合に行う作業は原則として以下。

- `app/page.tsx` の編集
- `app/globals.css` の編集
- 必要なWebコードの編集
- `public/images/` に既に存在する素材の利用・参照変更
- GitHubへのコミット

「イメージ図に合わせて」「この画像のように配置して」は画像生成指示ではない。イメージ図はWebデザインの参照資料として扱う。

## 2. 指示された変更をそのままGitHub上のサイトへ反映する

ホームページ編集の依頼を受けた場合、実装可能であれば説明だけで終わらず、対象ファイルを確認してGitHub上のコードを編集します。

ユーザーが「作業お願いします」「変更してください」「削除してください」など実行を求めた場合、原則として実際の変更まで行います。

## 3. 一度に一つの変更

ユーザーが一箇所の修正を指定した場合、その箇所だけ変更します。

- 勝手に他セクションを変更しない
- 勝手にコピーを書き換えない
- 勝手に画像を追加・生成しない
- 勝手にデザインを再構成しない

関連修正が技術的に必須の場合のみ、必要最小限の範囲で変更します。

## 4. MASTER / イメージ図の扱い

ユーザーが提示したMEMORIELAホームページのイメージ図は **デザインMASTER / 参照資料** です。

- 画像としてそのままWebページに貼らない
- 再生成しない
- MASTERに存在しない装飾・文字・要素を独自判断で追加しない
- HTML / CSS / 既存画像素材を使って再現する
- レスポンシブ対応ではMASTERの視覚的意図を維持する

## 5. 既存素材を優先する

`public/images/` に公式素材がある場合は、それを使用します。

素材不足でMASTERを適切に再現できない場合は、CSSによる極端な拡大・変形・無理なトリミングで誤魔化しません。必要な素材仕様をユーザーへ具体的に提示します。

## 6. 削除指示

ユーザーがWebページ上の要素を「消して」「削除して」と指示した場合、まずコード上の該当要素を削除します。

画像内の内容そのものを消す画像加工とは解釈しません。画像ファイル自体の加工が必要な場合は、その事実を説明してから扱います。

## 7. 現在のブランド前提

- Brand: `MEMORIELA`
- Project: `Project YURA`
- Character: `久遠ゆら / YURA`
- English tagline: `Memory shapes personality.`
- Japanese tagline: `記憶が、人格を形作る。`
- Concept copy: `AIと、心が通う物語。`
- Official X: `@MEMORIELA_JP`
- Official website: `memoriela.com`

旧ブランド表記・旧コピーへ独自判断で戻しません。

## 8. 作業前チェック

ホームページ編集時は、**ツール実行前に必ず**次の順序で確認する。

1. Project YURA / MEMORIELAのホームページ作業に関するメモリ / personal context を読む。
2. この `WEBSITE_EDITING_RULES.md` を読む。
3. **`docs/website/HOMEPAGE_DECISIONS.md` のACTIVE決定を読む。**
4. `docs/website/HOMEPAGE_MASTER_2026-09-19.md` を読む。
5. GitHub上の現在の `app/page.tsx` / `app/globals.css` / relevant assets を確認する。
6. ユーザーが変更を求めている対象を特定する。
7. ACTIVE Decisionと矛盾しないか確認する。矛盾して見える場合は、明示的な新しいユーザー決定がない限りACTIVE Decisionを維持する。
8. MASTERにその要素が存在するか確認する。
9. 指定箇所以外を変更していないか確認する。
10. **画像生成ツールを呼ぶ必要がないことを再確認する。**

ホームページ作業では、新規画像生成へ切り替えない。判断に迷った場合も生成せず、Web実装として処理する。


## 9. 既決事項の手戻り防止

ホームページ作業では、すでにユーザーとFIXした事項を、後続の別指示からAIが推測して上書きしない。

- 優先順位は **最新の明示的なユーザー決定 > HOMEPAGE_DECISIONS.md のACTIVE決定 > 最新MASTER文書 > 現在の実装 > AIの推測**。
- 「この部分を削除」「ここは別ページへ移す」など範囲指定がある場合、指定された対象だけを変更し、隣接要素まで勝手に含めない。
- スクリーンショットで複数要素が見えていても、ユーザーが指した対象と、すでにFIX済みの別要素を混同しない。
- 既決事項と新しい指示が矛盾して見える場合、AIの解釈で既決事項を変更しない。変更対象を限定したまま実装する。
- MASTER文書を更新するときは、ユーザーが実際に決めた内容だけを記録する。AIの推測を「最新FIX」として書き込まない。
- FIX済み仕様は `HOMEPAGE_DECISIONS.md` に追記する。既存Decisionは書き換えず、変更時は新Decisionで `Supersedes` を明示する。
- current mainの文書と過去のDecisionが食い違う場合、Git履歴を確認し、AI自身による誤更新かユーザーの明示的変更かを判定する。
- 作業前に「残すもの / 消すもの / 別ページへ移すもの」を内部的に照合し、FIX済み項目が意図せず消えていないか確認する。
